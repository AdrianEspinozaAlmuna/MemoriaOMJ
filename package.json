{
  "name": "memoria-omj-root",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd Frontend && npm run build",
    "test": "cd tests && npm test",
    "test:ordered": "cd tests && npm run test:ordered",
    "test:blackbox": "cd tests && npm run test:blackbox",
    "test:integration": "cd tests && npm run test:integration",
    "test:e2e": "cd tests && npm run test:e2e"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
